package com.peters.hackerrank;

import org.junit.Test;

import java.util.HashMap;
import java.util.Map;

import static org.junit.Assert.*;

public class HashTablesTest {

 /*   @Test
    public void shouldReturnYesIfCanUseMagazine() {
//        given
        String magazine[] = {"give", "me", "one", "grand", "today", "night"};
        String ransom[] = {"give", "one", "grand", "today"};
        Map<Integer, String> map = new HashMap<>();
        int counter = 0;
        for (String s : ransom) {
            map.put(counter++, s);
        }

//      when
//        String result = HashTables.ransomNote(magazine, map);

//        then
        assertEquals("Yes", result);
    }

    @Test
    public void shouldReturnNoIfCanNotUseMagazine() {
//        given
        String magazine[] = {"two", "times", "three", "is", "not", "four"};
        String ransom[] = {"two", "times", "two", "is", "four"};
        Map<Integer, String> map = new HashMap<>();
        int counter = 0;
        for (String s : ransom) {
            map.put(counter++, s);
        }

//      when
        String result = HashTables.ransomNote(magazine, map);

//        then
        assertEquals("No", result);
    }

//    apgo clm w lxkvg mwz elo bg elo lxkvg elo apgo apgo w elo bg
//    elo lxkvg bg mwz clm w
//    Yes

    @Test
    public void shouldReturnYesIfCanNotUseMagazine2() {
//        given
        String magazine[] = {"apgo", "clm", "w", "mwz", "elo", "bg", "elo", "lxkvg", "elo", "apgo", "apgo", "w", "elo","bg"};
        String ransom[] = {"elo", "lxkvg", "bg", "mwz", "clm", "w"};
        Map<Integer, String> map = new HashMap<>();
        int counter = 0;
        for (String s : ransom) {
            map.put(counter++, s);
        }

//      when
        String result = HashTables.ransomNote(magazine, map);

//        then
        assertEquals("Yes", result);
    }*/
}